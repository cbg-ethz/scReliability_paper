# Archived figure scripts

## figS_sensitivity.py

Sensitivity of the benchmark re-ranking to evaluation choices. Not part of the manuscript and ships no
figure. Restore by moving it back to `scripts/06_figures/` and adding it to `run_all.sh`; renumber it
first, as it currently labels itself Supplementary Figure S5.
